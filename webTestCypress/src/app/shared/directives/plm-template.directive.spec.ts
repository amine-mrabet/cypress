import { PlmTemplateDirective } from './plm-template.directive';

describe('PlmTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new PlmTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
